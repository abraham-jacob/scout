.idea/
# Leftover from the old Gmail OAuth integration (removed) — kept ignored as a
# safety net in case stale credential/token files still exist on disk.
credentials.json
token.json
Pipfile.lock
.env
__pycache__/
*.pyc
data/
logs/
.venv/
/htmlcov/
/junit_xml_test_report.xml
.coverage
mockups/
# Personal match-scoring artifacts (never commit)
profiles/*
!profiles/README.md
